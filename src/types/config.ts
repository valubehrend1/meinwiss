// global types for enumerations and configurations

/**
 * Supported languages in the application
 */
export enum Language {
  EN = 'en',
  DE = 'de',
}

/**
 * language selector orientations
 */
export enum LanguageSelectorOrientation {
  HORIZONTAL,
  DROPDOWN,
}
